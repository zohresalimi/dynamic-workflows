{
  "name": "@DeFlow/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-process specs. Depends on the published DeFlow package, not on @DeFlow/daemon, so they exercise the artefact users install.",
  "devDependencies": {
    "@DeFlow/testkit": "workspace:*",
    "@types/node": "catalog:",
    "DeFlow": "workspace:*",
    "playwright": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
