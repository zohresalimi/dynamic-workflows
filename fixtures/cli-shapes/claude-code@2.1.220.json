{
  "binary": "claude",
  "version": "2.1.220 (Claude Code)",
  "capturedAt": "2026-08-02",
  "capturedBy": "reading the installed binary's own --help and running it; transcribed into docs/07-provider-adapter-layer.md §8.1 on 2026-08-02, and into this file from that record",
  "note": "The one wire shape in this repository that was verified by execution against a real vendor CLI. packages/testkit's fake exec-shim agent is checked against it so the fake cannot drift into a shape no real Claude Code ever emitted.",
  "outputFormats": ["text", "json", "stream-json"],
  "streamJson": {
    "everyLineCarries": ["session_id", "uuid"],
    "lineTypes": ["system", "active_goal", "assistant", "rate_limit_event", "result"],
    "systemSubtypes": ["init", "commands_changed", "post_turn_summary"],
    "resultEnvelopeKeys": [
      "type",
      "subtype",
      "is_error",
      "stop_reason",
      "total_cost_usd",
      "usage",
      "modelUsage",
      "permission_denials",
      "terminal_reason",
      "result",
      "session_id",
      "uuid"
    ],
    "rateLimitEventKeys": ["type", "rate_limit_info", "session_id", "uuid"],
    "rateLimitInfoKeys": ["status", "resetsAt"],
    "verboseRequiredError": "Error: When using --print, --output-format=stream-json requires --verbose"
  }
}
