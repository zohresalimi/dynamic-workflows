/**
 * @DeFlow/web — the Vue 3 + Vite SPA: the ledger-projection Pinia store and the
 * nine P0 views.
 *
 * It imports from @DeFlow/core for types only. vue, pinia, @vue-flow/core, d3,
 * xterm.js and shiki arrive with EPIC-16.
 */
export {};
