{
  "extends": "../../tsconfig.base.json",
  // bin/ holds the DeFlow-mcp executable — the second bin of the published
  // DeFlow package (docs/07-provider-adapter-layer.md §7.2). It ships, so it
  // is typechecked like the rest of the package.
  "include": ["src", "bin"]
}
